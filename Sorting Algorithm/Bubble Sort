#include<bits/stdc++.h>
#define ll long long
#define pb push_back
using namespace std;

int main(){

    int n ;
    cin >> n ;
    int ar[n] ;

    for( int i = 0 ; i < n ; i++ )
        cin >> ar[i] ;
    //for( int i = 0 ; i < n ; i++ ) cout << ar[i] << " " ; cout << endl;

    for(int i = n-1 ; i >= 0 ; i-- ){
        for( int j = 0 ; j < i ; j++ ){
                
            if(ar[j] > ar[j+1] ) swap ( ar[j] , ar[j+1] ) ;
        
        }
    }

    for( int i = 0 ; i < n ; i++ ) cout << ar[i] << " " ; cout << endl;


return 0;
}
